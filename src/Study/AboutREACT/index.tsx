const Index = () => {
	return (
		<>
			1. StrictMode
			2. vite 의 역할
			3. rollup ? 번들러 ? 빌드 ? 컴파일 ?
			4. useReducer, useContext ?
			5. useMemo 와 useCallback 의 적절한 사용
		</>
	);
};

export default Index;